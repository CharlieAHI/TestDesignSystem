import 'package:flutter/material.dart';

class AppRadii {
  static const radiusButton = BorderRadius.all(Radius.circular(20));

  static const radiusNavigation = BorderRadius.all(Radius.circular(8));

  static const radiusInputList = BorderRadius.all(Radius.circular(8));

  static const radiusInput = BorderRadius.all(Radius.circular(4));

  static const radiusSheet = BorderRadius.all(Radius.circular(20));

  static const radiusDialog = BorderRadius.all(Radius.circular(20));

  static const radiusCard = BorderRadius.all(Radius.circular(4));

  static const radiusNotification = BorderRadius.all(Radius.circular(16));

  static const radiusInfoCard = BorderRadius.all(Radius.circular(16));

  static const radiusStarIndicator = BorderRadius.all(Radius.circular(100));

  static const radiusRisk = BorderRadius.all(Radius.circular(100));

  static const radiusConfidenceLevel = BorderRadius.all(Radius.circular(100));

  static const radiusCartridge = BorderRadius.all(Radius.circular(4));

  static const radiusFilterGraph = BorderRadius.all(Radius.circular(30));

  static const radiusResultsCard = BorderRadius.all(Radius.circular(8));

  static const radiusScanResults = BorderRadius.all(Radius.circular(4));

  static const radiusButtonNavigation = BorderRadius.all(Radius.circular(100));

  AppRadii._();
}
